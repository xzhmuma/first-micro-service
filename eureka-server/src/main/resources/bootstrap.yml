spring:
  application:
    name: eureka-service
  cloud:
    config:
      enabled: true
      discovery:
        enabled:  true
        service-id: config-service
      profile: dev
      label: master
security:
  basic:
    enabled: true
  user:
    name: chuchen  # 用户名
    password: chuchen1234   # 用户密码


